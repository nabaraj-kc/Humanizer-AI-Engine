The strategy below breaks your vision down into an exhaustive, multi-part engineering blueprint (PRD, TRD, UI/UX, Architecture, and Database Schemas) followed by a 50-Stage Granular Implementation Plan. Each stage is designed as an isolated, highly detailed prompt that you can feed to your Antigravity agent one by one. Every single stage includes an explicit Compulsory Verification and Anti-Crash Guardrail to ensure your system never breaks during assembly.Part 1: Product Requirements Document (PRD)1. Core Vision & Problem StatementAcademic text generated by AI suffers from low perplexity (highly predictable word choices) and uniform burstiness (flat sentence structures). Standard AI tools strip away the human nuance, while existing "humanizers" on the market destroy formatting, mix up citations, or cost significant amounts of money in API credits. This system provides a local, free-tier-optimized, layout-preserving web application that converts robotic academic drafts into text that reads as if written by a seasoned researcher.2. High-Level Feature SetVisual Geometry Parsing: Ingests an academic PDF, indexes every paragraph's spatial coordinates $(X, Y)$, font weights, sizes, and layout boundaries, and holds them in memory.Citation & Math Protection Layer: Auto-detects LaTeX inline formulas, block equations, and standard citation patterns (e.g., [1], (Author, Year)), extracting them into a secure key-value dictionary to prevent the LLM from rewriting or corrupting them.The Waterfall Free-API Router: A multi-provider middleware class that manages API keys for Google AI Studio, Groq, and DeepSeek, automatically handling failovers (HTTP 429 / 503) seamlessly without pausing execution.
Global Context Injection (Anti-Amnesia): Before the chunking and rewriting loops begin, the entire parsed PDF text is forwarded to a long-context model (Gemini 1.5 Pro). The model generates a structured 200-word Master Summary containing the paper's core thesis, primary methodology, and a strict 10-word technical glossary of domain-specific terms that must not be altered during rewriting. This Master Summary is persisted to the database and automatically injected into every per-chunk prompt by the Prompt Compiler, ensuring the AI never loses global document context across the fragmented processing pipeline.Asynchronous WebSocket State Machine: A backend event loop that broadcasts real-time status packets to the frontend UI, detailing which chunk is processing, current AI detector scores, and live token expenditures.Visual Node Status Dashboard: An elegant web interface that displays a live map of the agentic pipeline, illuminating active processing paths, looping iterations, and historical token burn metrics.Part 2: Technical Requirements Document (TRD)1. Full System Tech StackOperating Language: Python 3.11+ (Backend), JavaScript/HTML5/Tailwind CSS (Frontend).Primary Web Framework: FastAPI (Asynchronous ASGI server supporting high-throughput WebSockets).PDF Extraction & Re-rendering: PyMuPDF (fitz) combined with ReportLab or direct PDF canvas drawing.Local Adversarial Detector: transformers library executing a local, quantized roberta-base-openai-detector or deberta-v3-large fine-tuned for AI classification.State & Database Tracking: SQLite (Single-file, ultra-fast database for real-time token tracking and historical run logs).2. Mathematical Definition of Text TransformationThe system optimizes text to pass classification algorithms by dynamically shifting two variables:Perplexity ($P$): A measure of how likely an LLM would predict the next word. The system forces the text out of the lowest probability pathways:$$P(W) = P(w_1, w_2, \dots, w_N)^{-\frac{1}{N}} = \sqrt[N]{\prod_{i=1}^{N} \frac{1}{P(w_i \mid w_1, w_2, \dots, w_{i-1})}}$$Burstiness ($B$): The statistical variance of sentence lengths within a textual block. The engine forces structural chaos by maximizing sentence length standard deviation ($\sigma_L$) against mean sentence length ($\mu_L$):$$B = \frac{\sigma_L - \mu_L}{\sigma_L + \mu_L}$$Part 3: App Flow & UI/UX Architecture[User Uploads PDF] ──> [PyMuPDF Geometry Scan & Token Chunker]
                                  │
                                  ▼
                     [WebSocket Connection Opened]
                                  │
     ┌────────────────────────────┴────────────────────────────┐
     ▼                                                         ▼
[Frontend UI: Render Live Flow Map]           [Backend Engine: Execute Waterfall Loop]
     │                                                         │
     │ <─── [Real-Time WebSocket Event Stream JSON] ───────────┤
     │                                                         ▼
     │                                            [Is Local RoBERTa Score < 15%?]
     │                                             ├──> NO  ──> [Loop Back to LLM]
     │                                             └──> YES ──> [Save Valid Text Block]
     ▼                                                         │
[Update Live Token Meter & Gauges]                            ▼
     ▲                                            [Rebuild Layout Geometry on Canvas]
     │                                                         │
     └─────────────────────────────────────────────────────────┼──> [Export Final PDF]
UI/UX Layout SpecificationsScreen 1: Control Center: File drag-and-drop terminal with configuration sliders for target humanization depth. A sidebar displays API connection statuses for Google, Groq, and DeepSeek with green/red connectivity indicators.Screen 2: Live Operation Dashboard: A modern UI that appears once processing begins. The left panels contain a real-time event ticker. The center graphic depicts the node layout mapping out the active pipeline. The right panel houses an animated dashboard with dynamic gauges monitoring remaining free daily tokens.Part 4: Backend Schema & Database LayoutTo manage token budgets, track API quotas, and store text chunks during processing, the SQLite database utilizes the following relational schema:Table: api_quotasColumn NameData TypeConstraintsPurposeproviderTEXTPRIMARY KEYProvider identifier (google, groq, deepseek)daily_limitINTEGERNOT NULLHard token cap allowed per 24-hour cycleused_todayINTEGERDEFAULT 0Accumulated token count consumed since resetrpm_limitINTEGERNOT NULLRequests Per Minute throttle constraintlast_resetTIMESTAMPDEFAULT CURRENT_TIMESTAMPTimestamp marking last daily quota renewalTable: paper_runsColumn NameData TypeConstraintsPurposerun_idTEXTPRIMARY KEYUnique UUID generated per paper uploadfilenameTEXTNOT NULLOriginal filename of the uploaded research documenttotal_chunksINTEGERNOT NULLTotal segmentation blocks generatedstart_timeTIMESTAMPDEFAULT CURRENT_TIMESTAMPTimestamp when processing initiatedstatusTEXTCHECK IN ('running', 'completed', 'failed')Global execution statemaster_summaryTEXTNULLABLE200-word Master Summary + 10-word technical glossary generated by the Global Context Extractor before chunk processing beginsTable: text_chunksColumn NameData TypeConstraintsPurposechunk_idTEXTPRIMARY KEYUnique UUID mapping to a specific file blockrun_idTEXTREFERENCES paper_runsForeign key linking to parent execution contextsequence_noINTEGERNOT NULLSequential index to guarantee precise reassemblyraw_textTEXTNOT NULLOriginal raw text extracted from PDF layoutclean_textTEXTNOT NULLExtracted text stripped of temporary tokensprocessedTEXTDEFAULT NULLFinal verified text passing adversarial thresholditerationsINTEGERDEFAULT 0Total loop cycles executed through rewriting enginePart 5: Complete Project Blueprint README.mdMarkdown# Advanced Layout-Preserving AI Humanizer Engine (Antigravity Edition)

An asynchronous, local-first system designed to ingest academic PDFs, process text through a multi-provider free-tier API failover matrix, validate formatting output via an adversarial local detector, and re-render a structural clone of the original PDF layout.

## Features
- Structural geometry preservation via PyMuPDF coordinate mappings.
- Bulletproof regex protection walls for markdown syntax, equations, and inline citations.
- Live system state streaming over WebSockets into a web status console.
- Adaptive API routing with automated retry mechanisms for Google, Groq, and DeepSeek free tiers.

## System Pre-requisites
- Python 3.11 or higher.
- Modern web browser with WebSocket capability.
- Valid API keys for Google AI Studio, Groq, and/or DeepSeek.

## Quickstart Configuration
1. Initialize the project workspace directory tree.
2. Store your API credentials in a secure local environment template:
   ```bash
   cp .env.example .env
   # Open .env and input your free tier developer keys
Initialize the application backend stack:Bashpython main.py
Point your web browser to http://localhost:8000 to access the Control Center.